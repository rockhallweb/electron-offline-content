name: CI

on:
  push:
    branches:
      - main
  pull_request:
    types:
      - opened
      - synchronize
      - reopened
      - ready_for_review
  workflow_dispatch:
  merge_group:
    types:
      - checks_requested
    branches:
      - main

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  validate:
    name: validate (node ${{ matrix.node-version }})
    runs-on: blacksmith-4vcpu-ubuntu-2404
    timeout-minutes: 20
    strategy:
      fail-fast: false
      matrix:
        node-version: [24, 25]
    if: >-
      (github.event_name != 'pull_request' ||
       github.event.pull_request.head.repo.full_name == github.repository) &&
      (github.event_name != 'pull_request' ||
       github.event.pull_request.draft == false)

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up pnpm
        uses: pnpm/action-setup@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Run Turbo validation pipeline
        env:
          # Vitest workers default to ~2GB heaps; jsdom + large main tests can OOM on ubuntu-latest.
          NODE_OPTIONS: --max-old-space-size=6144
        # Smoke tests are headless; xvfb-run here caused long stalls on ubuntu-latest (Vitest + jsdom).
        run: pnpm validate

      - name: Pack verify (main only)
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'
        run: pnpm pack:verify

      - name: Install example dependencies
        run: |
          pnpm install --frozen-lockfile --ignore-scripts --dir examples/local &
          pnpm install --frozen-lockfile --ignore-scripts --dir examples/nasa &
          wait

      - name: Verify example apps
        run: pnpm examples:verify

  test-integration:
    name: test integration (main Vitest, node ${{ matrix.node-version }})
    runs-on: ubuntu-latest
    timeout-minutes: 20
    if: github.event_name == 'workflow_dispatch' || (github.event_name == 'push' && github.ref == 'refs/heads/main')
    strategy:
      fail-fast: false
      matrix:
        node-version: [24, 25]

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up pnpm
        uses: pnpm/action-setup@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Run main-process Vitest suite (integration)
        env:
          NODE_OPTIONS: --max-old-space-size=6144
        # Full `pnpm test` also runs renderer hook tests; those stall Vitest on ubuntu runners (see test:react locally).
        run: xvfb-run -a pnpm exec vitest run --config vitest.node.config.ts
